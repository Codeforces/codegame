import model.*

class MyStrategy {
    fun getAction(playerView: PlayerView, debug: Debug): Action {
        TODO("Write your strategy here")
    }
    fun debugUpdate(playerView: PlayerView, debug: Debug) {}
}
